# Delivery Specs — формат, звук, safe-зоны, QA

Технические цели вывода. Команды зависят от редактора — важны ЦЕЛИ ниже.

## 1. Формат
| назначение | разрешение | соотношение |
|---|---|---|
| **TikTok / Reels / Shorts / VK Клипы** | **1080×1920** | 9:16 — основной и единственный обязательный |
| YouTube (длинные разборы) | 1920×1080 | 16:9 — только если попросят отдельно |

Вся геометрия из `brand-kit.md` рассчитана на **1080×1920**. Для другого холста масштабировать
пропорционально, сохраняя проценты, а не пиксели.

## 2. FPS
- **Выходной fps = fps исходника.** Эталоны — 30 fps. Разный fps на вход/выход или на отдельных кусках →
  рассинхрон A/V. Типовые: 25 / 30 / 50 / 60.
- Кино-вставки часто идут 23.976 — их **конформить к fps проекта**, а не ресемплить проект под них.
- Не применять ресемпл к отдельным дублям — только крой и трим.

## 3. Длительность
- **Длительность задаёт исходник.** Речь не режется, поэтому хронометраж ролика = хронометраж дубля.
  Уложиться в «эталонные» 50–65с — задача записи, а не монтажа.
- Эталоны: 55.6с и 54.8с. Реальные ролики выходят и на 70+с — это нормально, не повод резать речь.

## 4. Safe-зоны
- Чёрная рамка вокруг карточки **уже даёт** запас: 105px слева/справа, 238px сверху, 303px снизу.
- **Текст никогда не выходит за карточку** — отступ ≥30px внутрь от её краёв (см. `brand-kit.md §5`).
- Нижние ~350px и верхние ~250px холста перекрываются интерфейсом TikTok/Reels — туда не ставить
  ничего критичного. Карточка A в эти зоны не заходит.
- Субтитры — **никогда поверх рта**. Базовая линия ≈ y1305 (68% высоты).

## 5. Звук
Эталоны отдают **-14.3 и -14.7 LUFS** интегрированной громкости при LRA 3.8–4.2 LU — это стандарт площадок.

- **Голос — главный и почти единственный.** Постоянной музыкальной подложки в эталонах нет:
  в паузах уровень падает до -25…-28 dB, тонального музыкального материала там не слышно.
- **Без дакинга.** Если добавляешь музыку — она просто тише голоса, а не «прыгает» под ним.
- **Голос не в лимитер.** Цель: **-14 LUFS интегрированно, истинный пик -1 dBTP**, LRA 3–5 LU.
- На склейках есть низкочастотные переходы: замерены всплески в полосе **25–90 Гц** на большинстве резов.
  Это **whoosh/суб-удар**, а не музыка.

### Уровни SFX — рабочие, проверенные на слух
Числа ниже — доля от **пикового** уровня голоса. Это уже откорректированные значения:
первая версия была втрое громче и **долбила в уши**, слушать было невозможно.

| звук | уровень | где |
|---|---|---|
| low whoosh (суб-переход) | **0.040** | на кадре реза |
| impact | **0.070** | на ревиле синего числа (R5b) |
| soft tick | **0.030** | на элементах инфографики (каскад R10), 3 штуки с шагом 85мс |
| ~~мягкий «пуф» появления текста~~ — **отменён автором (ролик 11): звука появления текста нет** | RMS **−16 дБ** от RMS голоса | шум с горбом ~1.9 кГц (≈0.9–4 кГц) + тихий призвук 1.1→1.3 кГц, атака 30мс; в характере soft tick двери. Пик — в момент всплытия первого слова фразы; ≈ раз в 3с (не чаще 2.2с), первая фраза — всегда. **Проверено:** резкий «вжух» как у образца (шум 5–12 кГц, −6 дБ) автор забраковал; такие верха ещё и поднимают истинный пик после AAC |

- **Сабовый свип в whoosh держать слабым** — не громче 0.45 от шумовой составляющей.
  Именно низ 25–90 Гц и создаёт ощущение «бьёт по ушам», даже когда общий уровень нормальный.
- Звук ставится **на кадр моушн-хита**, не «примерно рядом».
- Правило проверки: если SFX слышно как отдельное событие, а не как «подчёркивание реза» — тише.

### Финальный loudnorm — проверять по факту, а не по флагам
Однопроходный `loudnorm=I=-14:TP=-1.5:LRA=7` на этом материале **недобирает около 0.9 dB**:
отдаёт -15.1…-15.3 LUFS вместо -14. Двухпроходный с `linear=true` не спасает — линейное усиление
упирается в потолок по истинному пику и режется.

Рабочая схема: после loudnorm добавить `volume` и `alimiter=limit=0.80`, замерить готовый файл,
скорректировать усиление, повторить. Сходится за 2–3 итерации.

- **Мерить всегда готовый `.mp4`**, а не микс: loudnorm и AAC меняют и громкость, и истинный пик.
- **Проверять оба числа сразу.** Итерация может попасть в -14.2 LUFS и одновременно выбить пик
  до 0 dBFS — такую версию не отдавать, вернуть предыдущую, которая прошла по пику.
- Ориентир для варианта уже сданного ролика — громкость **оригинала**, а не абстрактные -14:
  зритель сравнивает версии между собой.

### Музыка
Фоновая подложка ставится **всегда**, если трек передан (папка `audios/`).

- Уровень: **−19 dB относительно RMS голоса**. Музыка должна ощущаться, но не соперничать.
- **Без дакинга.** Ровный уровень весь ролик.
- Трек обычно короче ролика — **зацикливать с кроссфейдом 0.25с на шве**, чтобы стык не был слышен.
- Фейд-ин **0.8с** в начале, фейд-аут **2.0с** в конце.
- Только своя или лицензированная музыка. Треки из соцсетей не использовать.

## 6. QA-чеклист перед сдачей

**Звук / склейки**
- [ ] Голосовая дорожка не резана: играет сплошняком от начала до конца, ни одного слышного стыка.
- [ ] Ни один рез не попал в середину слова (сверить по пословным таймингам).
- [ ] Субтитры совпадают с речью кадр в кадр.
- [ ] Нет мёртвого воздуха ≥3с без смены картинки.
- [ ] Интегрированно ≈ -14 LUFS, истинный пик ≤ -1 dBTP, без клиппинга.

**Версия с другим хуком (дополнительно)**
- [ ] От старого хука не осталось ни кадра, ни субтитра: рез прошёл по границе плана.
- [ ] A-roll хука собран штатным трактом: те же `FRAMINGS`, `hflip` и `GRADE`, без поправок под дубль.
- [ ] **Цветовые теги сверены** (`pix_fmt` / `color_space` / `color_transfer` / `color_primaries`)
      у хука и у основного исходника. Если разошлись — HDR приведён к SDR bt709 до тракта A-roll,
      `npl` подобран по средним RGB, а не на глаз.
- [ ] Средний RGB внутри карточки A на кадрах хука и тела расходится не больше чем на ~10 единиц.
- [ ] Зеркальность дубля проверена по надписи в кадре: штатный `hflip` не перевернул уже
      незеркальный экспорт.
- [ ] Если крупность, свет или ориентация дубля разошлись с основным исходником — расхождение
      измерено и названо в сдаче, а не подчищено кропом или лутом.
- [ ] На стыке нет щелчка: скачок между соседними сэмплами того же порядка, что внутри дорожки.
- [ ] Пауза между хуком и первой фразой тела 0.10–0.20с, как между предложениями в теле.
- [ ] Громкость совпадает с оригиналом ролика, а не только «примерно -14».
- [ ] Тело кадр в кадр совпадает с уже сданной версией.
- [ ] У каждой версии свой трек из `audios/`, ни один не повторяется.
- [ ] Деформация версии применена и **измерена**: кроп ×0.98 даёт ×1.0204 на экране.
- [ ] После ускорения fps ровно 30 (`r_frame_rate=30/1`), а не 30.6.
- [ ] Ускорение прошло и по картинке, и по звуку: длительности видео и аудио сходятся.
- [ ] Версия №1 не тронута — файл побайтово тот же, что сдавали.

**Картинка**
- [ ] Чёрная рамка на месте, карточка не растянута на весь холст.
- [ ] Центр карточек (540, 925) не прыгает между планами.
- [ ] Карточка не двигается и не зумит внутри плана.
- [ ] Все переходы — прямые резы, ни одного диссолва.
- [ ] Средняя длина плана 1.7–2.1с, ни один план не длиннее 6с.
- [ ] Лицо в кадре 30–45% хронометража. Для доказательных роликов ближе к 30% — норма.
- [ ] Нет планов и слоёв короче 1с (кроме одной очереди R3).
- [ ] Первый рез не позже 2.0с.
- [ ] Все вставки — видео. Стоп-кадров и фотографий нет (исключение — пруф R8).
- [ ] **Ни один сток-клип не использован дважды в одном ролике.** Свериться по монтажному листу;
      при повторе — заменить на другой клип (`anti-patterns.md`, «Графика и вставки»).

**Смысловая проверка графики** — обязательно глазами по контакт-листу
- [ ] Каждый графический план понятен без знания кода: объект похож на то, что произносится в речи.
- [ ] Геометрия верна: замкнутые фигуры действительно замкнуты; разомкнутые фигуры имеют видимый
      смысловой зазор; отрезки касаются точно в вершинах, а не «примерно рядом».
- [ ] Если схема использует координаты, оси, точки или область, показано, что кодирует каждая точка:
      есть исходная шкала/объект, метки `x/y`, проекции или подсвеченная пара `(x, y)`.
- [ ] Крупные числа, формулы и подписи проверены на полном раскрытии, а не только в начале анимации.
- [ ] На паузе зритель понимает, что означают точки, линии, области и зазоры; если нет — схему упростить
      или добавить визуальную связку, не дублирующую субтитр.

**Текст** — три первых пункта проверять машинно, не глазами
- [ ] **Ни один пиксель текста не выходит за карточку.** Скан всего ролика через каждые 3 кадра:
      всё ярче 200 вне прямоугольника карточки = брак. Ожидаемый результат — ноль кадров.
- [ ] **Ни одна пара строк внутри блока не пересекается.** Считать по реальным габаритам глифов
      (`font.getbbox`), а не по кеглю. Ожидаемый результат — ноль пар.
- [ ] **Графика не пересекает текст.** Слой элементов графики (без сетки-подложки) и слой субтитров
      рендерятся отдельно, сравниваются пиксельно по альфе >40. Общих пикселей быть не должно.
      Ожидаемый результат — ноль кадров.
- [ ] **Ни один субтитр не повторяет то, что написано графикой** в том же кадре
      (`brand-kit.md §5`, «Одно и то же не пишется дважды»). Проверять по монтажному листу.
Правила ниже — **Субтитры v3** (`brand-kit.md`, с 2026-09-24). Прежние пункты про строчные, блоки,
разброс, приглушение, печатную машинку и serif-курсив отменены автором.
- [ ] Без пунктуации, 2–4 слова на фразу, **КАПС**, SF Pro Expanded Black (ширина 150, вес 1000).
- [ ] На экране одна фраза, не больше 2 строк, кегль 62, **только внизу** карточки (сверху текста нет).
- [ ] Каждое слово **всплывает** на своём тайминге (подъём 28px + блюр 10→0 + 0→100% за 0.23с).
- [ ] Фраза исчезает за один кадр, без затухания.
- [ ] Ни один субтитр не наезжает на рот.
- [ ] Цветных слов-акцентов (красный #FF3B30 / бирюзовый #2DE1C2) немного — ≈1 на 8с.

**Цвет**
- [ ] Только чёрный / белый / синий #3B4BE8 (+ максимум один элемент #C3DB4E внутри инфографики).
- [ ] Синим окрашены только числа и результаты.
- [ ] Синих чисел ≤4 на ролик. Цифры графики — тем же шрифтом, что субтитры, без свечения, синий #5B7CFF.
- [ ] Крупные числа-ревилы читаются на паузе за <1с: не лежат поверх синей заливки, яркой линии
      или другой графики того же цвета. Если контраст слабый — разнести число и объект по разным зонам.
- [ ] (с 2026-09-24) Вставки светлые и без притемнения; белый текст с тенью читается на каждой.
- [ ] (с 2026-09-24) У текста тень, без свечения.

**Звук отдельно**
- [ ] SFX не слышны как самостоятельные события — только подчёркивают резы.
- [ ] Низа в whoosh не «бьёт по ушам» (проверить на наушниках).
- [ ] Музыка подложена и не соперничает с голосом (−19 dB от RMS речи).
- [ ] Звука появления текста нет (отменён автором, ролик 11).

**Приёмы**
- [ ] Крупного слова R6 нет (с 2026-09-24 текст только внизу).
- [ ] Пруф-вставка (R8) — не больше одной.
- [ ] Очередь быстрых резов (R3) — не больше одной.
