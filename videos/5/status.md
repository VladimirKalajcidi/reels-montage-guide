# Статус — ролик 5 («квантовые вычисления»)

**Скрипты сборки используют суффикс 39** — слоты 4 и 5 заняты чужим проектом
`reels_good` (см. `videos/3/status.md` для объяснения конвенции), первый
полностью свободный номер после 38 — 39. Папка с материалом — `videos/5/` как есть.

- [x] 1. Расшифровка — `source.json`/`.srt` (whisper medium, ru, word-level)
- [x] 2. Раскадровка — `storyboard39.py`, план согласован (пользователь: «да»)
- [x] 3. Материалы — `stock/` (17 видео с Mixkit, каждый использован ровно один раз,
      изолированы под этот ролик)
- [x] 4. Сборка — `quantum_edit.mp4` (`render39.py` + `sfx39.py`)
- [x] 5. QA базы — `qa39.py`, всё по нулям (текст за карточкой 0/549, наложение строк
      0/130, R6 поверх субтитра 0/8); громкость поправлена доп. проходом
      `volume=1.00dB,alimiter=limit=0.85` → -14.21 LUFS, пик -1.26 dBTP (однопроходный
      loudnorm недобрал ~0.9dB, как в ролике 3); контакт-лист (39 кадров) просмотрен глазами
- [x] 6. Монтажный лист — `montage-plan.md`
- [ ] 7. Хуки — не запрошены

## Продолжить в новой сессии
Пересобрать состояние (если нужно):
```
cd build && python3 render39.py && python3 sfx39.py
ffmpeg -y -i ../videos/5/quantum_edit.mp4 -c:v copy \
  -af "volume=1.00dB,alimiter=limit=0.85:level=disabled" -c:a aac -b:a 192k /tmp/v5.mp4 \
  && mv /tmp/v5.mp4 ../videos/5/quantum_edit.mp4
```
Гайд заново не перечитывать — качество уже проверено, QA по нулям.

## Правки после сдачи
—
